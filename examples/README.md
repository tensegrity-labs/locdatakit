# Examples

All files in this folder are synthetic test data.

Run with YAML places:

```bash
locdatakit trip-log --gpx examples/example.gpx --places examples/places.yaml
```

Run with Home Assistant zones export:

```bash
locdatakit trip-log --gpx examples/example.gpx --places examples/zones.json
```
